# CSE_151B_Competition

## GPU Type and Approximate Generation/Inference Time

Datahub was used so GPUs in use changed depending on what was available at the time. I switched between using RTX 6000 Blackwell with 24GB, or using a H100 with 20GB. As such the approximate generation/inference time depends on the 


## Model Weights

Simply used prompt engineering and tweaked hyperparameters. No need.

## How to call run_inference()

The Jupyter notebook that this was ran on has been converted to a python script named ```run_inference.py```. You simply need to run the script, preferably from this folder in order to ensure everything works as it should.